# ncki-poc
PoC demonstrating Non-Content Keystroke Inference — a side-channel technique that classifies sensitive input (passwords, 2FA codes, card numbers) using timing and behavioral typing patterns, without recording actual keystrokes.
